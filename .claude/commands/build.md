# Build

Generated by `agent-workflow-kit`. Edit the kit template or
`agent-workflow.config.json`, not this rendered file.

Read `AGENTS.md` first, then follow `.agents/skills/build/SKILL.md`.

Use this repo's configured build command:

```bash
cargo build
```

Do not substitute a global package-manager default.
