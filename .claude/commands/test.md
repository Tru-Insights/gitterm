# Test

Generated by `agent-workflow-kit`. Edit the kit template or
`agent-workflow.config.json`, not this rendered file.

Read `AGENTS.md` first, then follow `.agents/skills/test/SKILL.md`.

Use this repo's configured test command:

```bash
cargo test
```

Report exact commands and results.
